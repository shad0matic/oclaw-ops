# OpenAI API key (for embeddings in CLI tools)
OPENAI_API_KEY="sk-..."

# Postgres connection (CLI tools use unix socket directly, this is for reference)
PGDATABASE="openclaw_db"
PGHOST="/var/run/postgresql"
